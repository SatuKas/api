services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: satukas-api
    restart: unless-stopped
    ports:
      - '${PORT:-5000}:5000'
    env_file:
      - .env
